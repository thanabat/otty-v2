{
  "name": "@otty/shared",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests configured for @otty/shared yet\""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

